{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'image</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_image_index') }}">Retour à la liste d'images</a>
        </li>
    </ul>
{% endblock %}
